server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://8.137.124.254:3306/score_check?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: zhangyiwen0612
    driver-class-name: com.mysql.cj.jdbc.Driver

  mvc:
    format:
      date: yyyy-MM-dd
      date-time: yyyy-MM-dd HH:mm:ss

# MyBatis-Plus 配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underline-to-camel-case: true # 开启驼峰命名转换
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 在控制台打印 SQL 语句
  global-config:
    db-config:
      id-type: auto # 主键自增
